// Generated by solarsql build. Do not edit.
// After a change to a statement, run `npx solarsql build`, and this file follows.
// Module repository-versions.
import type { Id, Meta } from "solarsql";

// The id of a row of repository_versions.
export type RepositoryVersionsId = Id<"repository_versions">;

export type Generated = {
  "insert or ignore into repository_versions\n    (id, root, repository_id, project_path, ecosystem, kind, dependency_role, origin, name, requested, locked, source, locator, status, detail)\n    select value ->> 'id', value ->> 'root', value ->> 'repository_id', value ->> 'project_path', value ->> 'ecosystem', value ->> 'kind',\n      value ->> 'dependency_role', value ->> 'origin', value ->> 'name', value ->> 'requested', value ->> 'locked', value ->> 'source', value ->> 'locator',\n      value ->> 'status', value ->> 'detail'\n    from json_each(:rows)": {
    params: { rows: readonly { "id": RepositoryVersionsId; "root": string; "repository_id": string | null; "project_path": string; "ecosystem": string; "kind": string; "dependency_role": "runtime" | "development" | "optional" | "peer" | null; "origin": "manifest" | "lock" | "version-file" | "source"; "name": string; "requested": string | null; "locked": string | null; "source": string; "locator": string | null; "status": "observed" | "unresolved" | "unsupported" | "error"; "detail": string | null }[] };
    row: {};
  };
  "select project_path, ecosystem, kind, dependency_role, origin, name, requested, locked, source, locator, status, detail\n    from repository_versions where root = :root\n    order by project_path, ecosystem, kind, name, source, locator, locked": {
    params: { root: string };
    row: { project_path: string; ecosystem: string; kind: string; dependency_role: "runtime" | "development" | "optional" | "peer" | null; origin: "manifest" | "lock" | "version-file" | "source"; name: string; requested: string | null; locked: string | null; source: string; locator: string | null; status: "observed" | "unresolved" | "unsupported" | "error"; detail: string | null };
  };
  "select root, project_path, ecosystem, kind, dependency_role, origin, name, requested, locked,\n      source, locator, status, detail\n    from repository_versions where kind = 'source' or status <> 'observed'\n    order by root, project_path, source, locator, kind, name": {
    params: {};
    row: { root: string; project_path: string; ecosystem: string; kind: string; dependency_role: "runtime" | "development" | "optional" | "peer" | null; origin: "manifest" | "lock" | "version-file" | "source"; name: string; requested: string | null; locked: string | null; source: string; locator: string | null; status: "observed" | "unresolved" | "unsupported" | "error"; detail: string | null };
  };
  "select root, project_path, source,\n      cast(sum(kind = 'source' and status = 'observed') as integer) as observed_sources,\n      cast(sum(kind = 'source' and status = 'unsupported') as integer) as unsupported_sources,\n      cast(sum(status = 'unresolved') as integer) as unresolved_evidence,\n      cast(sum(status = 'error') as integer) as error_evidence,\n      cast(sum(kind <> 'source' and status = 'observed') as integer) as observed_evidence\n    from repository_versions group by root, project_path, source\n    order by root, project_path, source": {
    params: {};
    row: { root: string; project_path: string; source: string; observed_sources: number | null; unsupported_sources: number | null; unresolved_evidence: number | null; error_evidence: number | null; observed_evidence: number | null };
  };
  "with declarations as (\n      select * from repository_versions\n      where ecosystem = 'npm' and kind = 'dependency' and origin = 'manifest'\n        and status = 'observed' and requested is not null and repository_id is not null\n    ), shared as (\n      select ecosystem, name,\n        cast(count(distinct repository_id) as integer) as repository_count,\n        cast(count(distinct root) as integer) as checkout_count,\n        cast(count(distinct root || char(0) || project_path) as integer) as project_count,\n        cast(count(*) as integer) as declaration_count,\n        cast(count(distinct requested) as integer) as request_string_count,\n        cast((select json_group_array(requested) from (\n          select distinct requested from declarations d2\n          where d2.ecosystem = declarations.ecosystem and d2.name = declarations.name order by requested\n        )) as text) as requested_versions,\n        cast((select json_group_array(dependency_role) from (\n          select distinct dependency_role from declarations d3\n          where d3.ecosystem = declarations.ecosystem and d3.name = declarations.name order by dependency_role\n        )) as text) as roles\n      from declarations\n      group by ecosystem, name having count(distinct repository_id) > 1\n    ) select ecosystem, name, repository_count, checkout_count, project_count, declaration_count,\n        request_string_count, requested_versions, roles\n      from shared order by repository_count desc, project_count desc, ecosystem, name": {
    params: {};
    row: { ecosystem: string; name: string; repository_count: number; checkout_count: number; project_count: number; declaration_count: number; request_string_count: number; requested_versions: string | null; roles: string | null };
  };
  "with declarations as (\n      select * from repository_versions\n      where ecosystem = 'npm' and kind = 'dependency' and origin = 'manifest'\n        and status = 'observed' and requested is not null and repository_id is not null\n    ), shared as (\n      select ecosystem, name,\n        cast(count(distinct repository_id) as integer) as repository_count,\n        cast(count(distinct root) as integer) as checkout_count,\n        cast(count(distinct root || char(0) || project_path) as integer) as project_count,\n        cast(count(*) as integer) as declaration_count,\n        cast(count(distinct requested) as integer) as request_string_count\n      from declarations\n      group by ecosystem, name having count(distinct repository_id) > 1\n    ) select d.ecosystem, d.name, d.root, d.project_path, d.dependency_role, d.requested,\n        d.source, d.locator, s.repository_count, s.checkout_count, s.project_count, s.declaration_count,\n        s.request_string_count\n      from declarations d join shared s on s.ecosystem = d.ecosystem and s.name = d.name\n      order by d.ecosystem, d.name, d.root, d.project_path, d.dependency_role, d.source, d.locator": {
    params: {};
    row: { ecosystem: string; name: string; root: string; project_path: string; dependency_role: "runtime" | "development" | "optional" | "peer" | null; requested: string | null; source: string; locator: string | null; repository_count: number; checkout_count: number; project_count: number; declaration_count: number; request_string_count: number };
  };
};

export const generated: Meta<Generated> = {
  "insert or ignore into repository_versions\n    (id, root, repository_id, project_path, ecosystem, kind, dependency_role, origin, name, requested, locked, source, locator, status, detail)\n    select value ->> 'id', value ->> 'root', value ->> 'repository_id', value ->> 'project_path', value ->> 'ecosystem', value ->> 'kind',\n      value ->> 'dependency_role', value ->> 'origin', value ->> 'name', value ->> 'requested', value ->> 'locked', value ->> 'source', value ->> 'locator',\n      value ->> 'status', value ->> 'detail'\n    from json_each(:rows)": { params: ["rows"], encode: ["rows"], json: [], reads: [] },
  "select project_path, ecosystem, kind, dependency_role, origin, name, requested, locked, source, locator, status, detail\n    from repository_versions where root = :root\n    order by project_path, ecosystem, kind, name, source, locator, locked": { params: ["root"], encode: [], json: [], reads: ["repository_versions"] },
  "select root, project_path, ecosystem, kind, dependency_role, origin, name, requested, locked,\n      source, locator, status, detail\n    from repository_versions where kind = 'source' or status <> 'observed'\n    order by root, project_path, source, locator, kind, name": { params: [], encode: [], json: [], reads: ["repository_versions"] },
  "select root, project_path, source,\n      cast(sum(kind = 'source' and status = 'observed') as integer) as observed_sources,\n      cast(sum(kind = 'source' and status = 'unsupported') as integer) as unsupported_sources,\n      cast(sum(status = 'unresolved') as integer) as unresolved_evidence,\n      cast(sum(status = 'error') as integer) as error_evidence,\n      cast(sum(kind <> 'source' and status = 'observed') as integer) as observed_evidence\n    from repository_versions group by root, project_path, source\n    order by root, project_path, source": { params: [], encode: [], json: [], reads: ["repository_versions"] },
  "with declarations as (\n      select * from repository_versions\n      where ecosystem = 'npm' and kind = 'dependency' and origin = 'manifest'\n        and status = 'observed' and requested is not null and repository_id is not null\n    ), shared as (\n      select ecosystem, name,\n        cast(count(distinct repository_id) as integer) as repository_count,\n        cast(count(distinct root) as integer) as checkout_count,\n        cast(count(distinct root || char(0) || project_path) as integer) as project_count,\n        cast(count(*) as integer) as declaration_count,\n        cast(count(distinct requested) as integer) as request_string_count,\n        cast((select json_group_array(requested) from (\n          select distinct requested from declarations d2\n          where d2.ecosystem = declarations.ecosystem and d2.name = declarations.name order by requested\n        )) as text) as requested_versions,\n        cast((select json_group_array(dependency_role) from (\n          select distinct dependency_role from declarations d3\n          where d3.ecosystem = declarations.ecosystem and d3.name = declarations.name order by dependency_role\n        )) as text) as roles\n      from declarations\n      group by ecosystem, name having count(distinct repository_id) > 1\n    ) select ecosystem, name, repository_count, checkout_count, project_count, declaration_count,\n        request_string_count, requested_versions, roles\n      from shared order by repository_count desc, project_count desc, ecosystem, name": { params: [], encode: [], json: [], reads: ["repository_versions"] },
  "with declarations as (\n      select * from repository_versions\n      where ecosystem = 'npm' and kind = 'dependency' and origin = 'manifest'\n        and status = 'observed' and requested is not null and repository_id is not null\n    ), shared as (\n      select ecosystem, name,\n        cast(count(distinct repository_id) as integer) as repository_count,\n        cast(count(distinct root) as integer) as checkout_count,\n        cast(count(distinct root || char(0) || project_path) as integer) as project_count,\n        cast(count(*) as integer) as declaration_count,\n        cast(count(distinct requested) as integer) as request_string_count\n      from declarations\n      group by ecosystem, name having count(distinct repository_id) > 1\n    ) select d.ecosystem, d.name, d.root, d.project_path, d.dependency_role, d.requested,\n        d.source, d.locator, s.repository_count, s.checkout_count, s.project_count, s.declaration_count,\n        s.request_string_count\n      from declarations d join shared s on s.ecosystem = d.ecosystem and s.name = d.name\n      order by d.ecosystem, d.name, d.root, d.project_path, d.dependency_role, d.source, d.locator": { params: [], encode: [], json: [], reads: ["repository_versions"] },
};
